ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [
           io.nais.devrapid.LeadtimeKt.leadtime
        ]
    }
    environment = ${?ktor_environment}
}
nais {
}

